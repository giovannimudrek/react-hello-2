I need more information to generate a useful System Prompt. The directory tree provided (`index.html` only) is insufficient. 

Could you clarify:

1. **Is this a new project setup?** If so, what technology stack should it use?
2. **Or should I analyze the existing repository?** I can explore the current working directory (`/Users/user/Documents/poc-agentes/claude-code-demo`) and its subprojects to generate a comprehensive CLAUDE.md.
3. **What is the scope?** Should the CLAUDE.md cover:
   - Just the backend folder?
   - The entire monorepo (backend + frontend + e2e)?
   - A specific sub-project?

**Option:** If you'd like, I can use the Explore agent to quickly scan the repository structure and generate a tailored CLAUDE.md based on actual code patterns detected. This would be faster and more accurate.

What would you prefer?

## Squad Workflow

Pipeline:
1. Agent Spec Writer
2. Figma to Code
