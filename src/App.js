/**
 * App.js — Entry point do projeto Task View
 *
 * Inicializa o BoardApp na rota /board (padrao).
 * Para expandir com React Router, importe este modulo e registre as rotas.
 */

// O BoardApp e inicializado diretamente pelo index.html
// Este arquivo serve como documentacao do ponto de entrada da aplicacao.

// Quando migrar para React:
// import React from 'react';
// import { createRoot } from 'react-dom/client';
// import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
// import BoardPage from './pages/BoardPage';
//
// const root = createRoot(document.getElementById('app'));
// root.render(
//   <BrowserRouter>
//     <Routes>
//       <Route path="/" element={<Navigate to="/board" replace />} />
//       <Route path="/board" element={<BoardPage />} />
//     </Routes>
//   </BrowserRouter>
// );
